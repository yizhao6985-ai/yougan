auto-install-peers=true
strict-peer-dependencies=false

# 国内网络访问 npmjs 易超时，使用 npmmirror（原淘宝镜像）
registry=https://registry.npmmirror.com

# 降低并发、延长超时，减少 ETIMEDOUT
fetch-timeout=600000
fetch-retries=5
network-concurrency=4
