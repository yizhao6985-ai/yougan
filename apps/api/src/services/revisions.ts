export {
  detectRevisionKind,
  emptySnapshot,
  materializeWorkColumns,
  parseBriefJson as parseBrief,
  parseOutlineJson as parseOutline,
  parsePlanJson as parsePlan,
  parseWorkDraft as parseDraft,
  parseWorkProfile as parseProfile,
  parseSnapshot,
  revisionSummary,
  snapshotFromAgentValues,
  snapshotsEqual,
} from "@yougan/domain";
